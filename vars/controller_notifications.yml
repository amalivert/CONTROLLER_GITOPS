---
controller_notifications:
  - name: Email notification
    description: Send out emails for controller jobs
    organization: secadm
    notification_type: email
    notification_configuration:
      username: 'admin'  # this is required even if there's no username
      sender: controller0@example.com
      recipients:
      - admin@example.com
      use_tls: false
      host: smtp.example.com
      use_ssl: false
      password: 'redhat123'  # this is required even if there's no password
      port: 25